# Contributing to Velora

Velora is an early prototype. Before opening a substantial change, please open
an issue describing the goal and how it preserves the project's non-destructive
Omarchy integration boundary.

Keep Linux integration in Rust, presentation in Godot, and never add arbitrary
shell-command IPC. Run `./scripts/check.sh` before submitting Rust changes.
